# Non-secret DR config for lab-node18. Committed to the repo, copied by the
# pipeline to /opt/dr/lab-node18/config.env. NEVER put secrets here.
APP_ENV=dr
APP_NAME=lab-node18-dr
DB_HOST=db-dr.drlab.internal
